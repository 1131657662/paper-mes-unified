# Install as /etc/sudoers.d/paper-mes-backup-verify with mode 0440.
# The wrapper validates the backup id and always targets an isolated database.
paper-mes ALL=(root) NOPASSWD: /usr/local/sbin/verify-paper-mes-backup-root ????????-??????
