# 本地上线验收报告（2026-07-15）

## 一、结论

当前代码已通过本地业务、测试、构建、性能和浏览器验收，具备进入正式发布前检查阶段的条件。

本报告不代表云端已经发布，也不把以下外部事项伪装为完成：

- 真实异地存储尚未配置，当前只完成复制脚本和成功/失败状态转换测试。
- 云端 systemd 监控、Webhook 和 Ubuntu 备份需要发布获批后安装并演练。
- 本地历史测试单 `JG202606230004` 的作废需要业务负责人确认。

未经明确确认，不提交 GitHub、不更新云端、不修改生产数据。

## 二、加工单双版本打印

| 验收项 | 证据 | 结论 |
| --- | --- | --- |
| 下发冻结版本 | `ProcessOrderPrintViewReaderTest`、`ProcessOrderPrintViewBusinessFlowIT` | 通过 |
| 完工实际版本 | `StandardSawBusinessFlowIT`、前端 `printPreviewModel.test.ts` | 通过 |
| 历史快照兼容 | `issuedVersion_withLegacySnapshot_filtersLaterFinishesAndClearsActuals` | 通过 |
| 损坏快照禁止回退读取实时数据 | `issuedVersion_withCorruptedSnapshot_rejectsLiveFallback` | 通过 |
| 首打、补打、只读打印模式 | 前端 `printIssueMode.test.ts` | 通过 |
| 并发首打只生成一次 | `ProcessOrderPrintConcurrencyBusinessFlowIT` | 通过 |
| 补打原因进入审计 | `reprint_withReason_recordsAuditableOperation` | 通过 |
| 页面打印预览 | 本地浏览器实测下发版/完工版切换、历史兼容提示和打印份数 | 通过 |

## 三、代表性业务流程

真实 MySQL 集成测试共 19 个测试类、51 项场景，全部通过。

| 业务类型 | 自动化证据 | 结论 |
| --- | --- | --- |
| 标准锯纸、双成品和切边 | `StandardSawBusinessFlowIT` | 通过 |
| 改门幅 | `StandardRewindBusinessFlowIT` 模式 1 | 通过 |
| 改直径 | `StandardRewindBusinessFlowIT` 模式 2 | 通过 |
| 门幅与直径同时变更 | `StandardRewindBusinessFlowIT` 模式 3 | 通过 |
| 分层复卷 | `StandardRewindBusinessFlowIT` 模式 4 | 通过 |
| 多母卷合并复卷 | `MultiSourceRewindBusinessFlowIT` | 通过 |
| 多道工序 | `MultiStageOrderBusinessFlowIT` | 通过 |
| 现场定尺锯纸、动态成品和切边 | `BackRecordOnSiteBusinessFlowIT` | 通过 |
| 现场定尺复卷 | `OnSiteRewindBusinessFlowIT` | 通过 |
| 不加工直发 | `DirectShipBusinessFlowIT` | 通过 |
| 成品与余料同时出库 | `FullLifecycleBusinessFlowIT` | 通过 |
| 出库改单、取消和释放库存 | `DeliveryEditBusinessFlowIT` | 通过 |
| 确认、回退、结算作废和库存恢复 | `DeliverySettleRollbackBusinessFlowIT` | 通过 |
| 签收、结算和收款闭环 | `FullLifecycleBusinessFlowIT` | 通过 |
| 重复确认、回退和作废 | `DeliveryStateBusinessFlowIT` | 通过 |
| 完工后回退编辑并重新完成 | `ProcessOrderRollbackEditBusinessFlowIT` | 通过 |

## 四、权限与高风险审计

- 后端 `RolePermissionMatrixContractTest` 共 6 项，覆盖管理员、制单员、回录员、出库员、财务和只读人员。
- 前端 `permissionMeta.test.ts` 共 6 项，与后端岗位语义一致。
- 只读人员不能执行核心业务写操作。
- 出库员不能办理结算，财务不能办理出库，回录员不能制单下发。
- 加工单回退/作废/补打/回录、出库确认/回退/取消、结算收款/取消收款/作废、备份/恢复演练/删除/清理和数据修复均有审计契约测试。

结论：前后端权限矩阵及高风险操作留痕通过。

## 五、数据健康与修复保护

- 数据巡检覆盖生产实际值缺失、现场定尺门幅无效、来源母卷缺失、出库快照损坏、结算快照损坏和金额不一致。
- `DataHealthBusinessFlowIT` 验证异常发现和受控修复。
- `SnapshotIntegrityBusinessFlowIT` 验证损坏快照停止读取及严重告警。
- 本地严重异常已有独立方案：`docs/本地严重数据异常修复方案-2026-07-15.md`。
- 修复前要求手动备份、隔离恢复演练、只读证据留存和完整操作日志。

未完成：`JG202606230004` 在业务确认属于测试单前不执行作废。

## 六、备份恢复与监控

| 能力 | 当前证据 | 结论 |
| --- | --- | --- |
| Windows 手动与自动备份 | 当前恢复点 `20260715-105227` 已生成，最近自动备份成功 | 通过 |
| 自动调度、任务恢复、并发锁 | backup 单元测试及任务历史 | 通过 |
| 保留期清理和手动删除保护 | `BackupMaintenanceServiceTest` | 通过 |
| 完整性校验和隔离恢复 | Windows/Bash 校验脚本及恢复演练记录 | 通过 |
| Ubuntu 脚本安全约束 | Bash 语法、契约和恢复清理保护测试 | 代码通过 |
| 异地复制 | `test-sync-backups-rclone.sh` 成功/失败行为测试 | 本地模拟通过 |
| 健康与告警 | `/actuator/health`、监控状态机和 systemd 模板验证 | 本地通过 |

未完成：真实 rclone 远端复制、从异地副本恢复、云端 Webhook 故障/恢复告警。

## 七、性能、并发与幂等

性能隔离库曾生成 100,000 张加工单及对应母卷、成品、工序和来源关系，业务库保持 82 张加工单不变。

- 首屏约 362 ms，第 5000 页约 1,009 ms。
- 10 万级报表 SQL 最大约 6.62 秒，没有超过 10 秒的单条 SQL。
- 单路 10 万行 Excel 导出约 22.0 秒、峰值堆内存 222 MB。
- 双路并发导出约 25.1 秒、峰值堆内存 344 MB。
- 并发方案保存、并发打印、单号生成、备份锁和业务重复操作均通过。

详细证据见 `docs/本地性能基线报告-2026-07-15.md`。

## 八、UI/UX 与可访问性

2026-07-15 使用本地浏览器在高度 900px 下重新验证：

| 页面 | 1366 | 1600 | 1920 | 2560 |
| --- | ---: | ---: | ---: | ---: |
| 加工单详情页面级横向溢出 | 0 | 0 | 0 | 0 |
| 新建出库页面级横向溢出 | 0 | 0 | 0 | 0 |
| 数据安全页页面级横向溢出 | 0 | 0 | 0 | 0 |

- 新建出库选择区与固定底栏保持 14px 间隔，底栏底部保持 16px，四档宽度均可见且不重叠。
- 数据安全表格始终位于主内容区内。
- 加工单详情、打印预览和数据安全页浏览器控制台 0 错误。
- 表单防丢失、加载失败提示、交互控件名称、键盘操作、备份控件名称均有前端测试。
- 认证状态码调整后再次使用本地浏览器验证：管理员登录、仪表盘、82 张加工单列表、数据安全和数据巡检均正常加载，会话失效后返回登录页。
- 使用一次性本地测试用户验证真实 HTTP 403，测试结束后用户和会话记录均已删除，无数据库残留。
- 已将废弃的 `InputNumber addonAfter` 全部替换为 `suffix`；前端重启后的运行日志不再出现对应 `console.error`。

## 九、当前质量门禁

| 门禁 | 结果 |
| --- | --- |
| 后端单元与契约测试 | 461 项通过，0 失败 |
| 真实 MySQL 业务流集成测试 | 51 项通过，0 失败 |
| 前端测试 | 19 个文件、57 项通过 |
| TypeScript 严格检查 | 生产构建通过 |
| 前端 Lint | 0 错误、0 警告 |
| 前端生产构建 | 通过 |
| Bash 监控和异地同步行为测试 | 通过 |
| `V2.8` 重复库存占用迁移保护 | 迁移停止，2 条明细均未删除，隔离库已清理 |
| 发布前只读预检 | 健康、备份校验、运行任务和重复业务数据检查通过行为测试 |
| 当前本地发布候选备份 | `20260715-105227`，SHA-256、解压和隔离恢复通过；恢复库含 27 张表、82 张加工单并已自动删除 |
| 生产配置隔离启动 | 固定隔离库验证 loopback、Actuator、鉴权和安全 Cookie |
| 本地认证状态码 | 未登录 `/api/auth/me` 为 HTTP 401，操作员访问受限数据巡检接口为 HTTP 403，`/actuator/env` 为 HTTP 404 |
| systemd 单元结构检查 | 通过 |
| `git diff --check` | 通过 |
| 本地后端健康检查 | HTTP 200、状态 UP |
| 浏览器控制台和最终日志 | 无错误、无废弃组件警告，无 SQL 参数或敏感认证信息输出 |

真实 MySQL 集成测试使用独立的 `paper_processing_test`，密码仅通过当前测试进程的 `PAPER_MES_IT_DB_PASSWORD` 注入，不写入仓库。测试结束后再次核对：业务库仍为 82 张加工单，业务库和测试库的 `IT%` 客户残留均为 0，性能隔离库残留为 0。

## 十、发布前剩余清单

- [ ] 业务负责人确认 `JG202606230004` 是测试单还是实际业务单。
- [ ] 配置真实 rclone 远端并核对异地文件与校验值。
- [ ] 从真实异地副本执行一次隔离恢复演练。
- [ ] 获准发布后在云端安装监控 timer，配置 Webhook 并完成故障/恢复演练。
- [ ] 正式发布窗口内重新生成数据库与上传文件备份；当前本地发布候选已生成并验证 `20260715-105227`。
- [ ] 用户明确确认后再提交 GitHub 和更新云端。

## 十一、本地变更清单

- 加工单：双版本打印、历史快照兼容、现场定尺动态成品与切边、来源母卷和完成快照完善。
- 出库：按加工单分组选择、来源母卷展示、成品与余料共同提货、待确认改单/取消和库存释放。
- 结算：收款、取消收款、作废和出库回退边界强化。
- 数据安全：Windows/Ubuntu 备份脚本、自动调度、任务历史、保留期清理、恢复演练和异地同步状态。
- 数据健康：生产、库存、出库、结算和快照一致性巡检及受控修复。
- 权限审计：六岗位前后端权限矩阵及高风险操作日志。
- UI/UX：表格、宽屏布局、固定底栏、空状态、错误状态、表单防丢失、键盘操作和可访问名称。
- 运维：Actuator 健康接口、状态变化告警、systemd timer、发布顺序、回滚和上线检查材料。

数据库增量迁移为 `V2.8`（运行安全基础设施）、`V2.9`（启用档案编码唯一约束）和 `V3.0`（加工单列表索引）。迁移前必须检查重复库存占用和重复启用编码；迁移遇到冲突会停止，不会自动删除业务单据。

在以上事项完成前，总目标保持执行中，不标记为生产上线完成。
