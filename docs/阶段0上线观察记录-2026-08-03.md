# 阶段 0 上线观察记录（2026-08-03）

## 观察边界

- 阶段 0 关闭时间：`2026-08-03T09:48:30+08:00`。
- 本次首次基线采集：`2026-08-03T10:37:44+08:00`，约为关闭后 T+49 分钟。
- 本记录只包含生产只读检查，不执行迁移、开账、库存写入、服务重启或配置变更。
- 24/48/72 小时观察尚未到期；本次结果只能作为初始基线，不能提前宣称整个观察周期完成。

## 初始基线结果

| 检查项 | 结果 |
| --- | --- |
| 后端服务 | `active`，`NRestarts=0`；主进程自 `2026-08-03 09:34:34 CST` 运行 |
| 后端健康 | `{"status":"UP","groups":["liveness","readiness"]}` |
| 生产源码 | `52dca07a8ae0b8cdd05eca36a870f0b6b56d4465`，工作区干净 |
| 公网入口 | `/` 返回 200；未认证 `/api/auth/me` 返回 401 |
| 迁移守卫 | 72 个 migration 全部 `applied`，源码 checksum 一致 |
| 服务日志 | 阶段 0 关闭后 `paper-mes` warning 及以上为 0，异常关键字扫描为 0 |
| Nginx 错误 | 最新错误停留在冻结窗口内的 `09:46:19`；关闭时间后未发现新错误 |
| 切换备份 | `/opt/backups/paper-mes/20260803-092818` 的 `SHA256SUMS` 与 gzip 检查通过 |
| 恢复证据 | 已验证 62 张表、44 张加工单，隔离恢复库验证后删除 |

前端焦点修复已部署到生产源码提交 `52dca07`，生产资源 SHA-256 为 `d1188a62992530b29c69487fd8f5971fae70e88c247dfd91602ecba08c3f562f`；该次为前端静态资源部署，后端未重启。前端回滚目录为 `/opt/paper-mes/frontend/dist.previous-52dca07-20260803-102506`。

## 台账只读复核

| 指标 | 结果 |
| --- | ---: |
| 台账总流水 | 721 |
| `OPENING_BALANCE` | 721 |
| 其他事件 | 0 |
| 开账实物重量 | 41900.000 kg |
| 开账预占重量 | 10780.000 kg |
| 开账可用重量 | 31120.000 kg |
| 负余额行 | 0 |
| 余额方程不一致 | 0 |
| 台账孤儿成品引用 | 0 |
| 重复开账卷 | 0 |
| 活跃预占孤儿 | 0 |

当前生产有 65 个在库成品卷，活跃预占重量仍为 `10780.000 kg`。观察时没有新库存业务流水，因此开账总量保持不变；后续出现 `RECEIPT/RESERVE/RELEASE/ISSUE/RETURN/SCRAP/ADJUSTMENT` 时，应按真实业务解释，不能简单要求“非开账流水永远为 0”。

## 后续观察门禁

| 节点 | 最早时间 | 状态 | 必查项 |
| --- | --- | --- | --- |
| T+24h | `2026-08-04T09:48:30+08:00` | 待执行 | 服务重启数、健康、warning/error、备份任务、迁移守卫、台账余额链、孤儿/重复/负余额 |
| T+48h | `2026-08-05T09:48:30+08:00` | 待执行 | 同上，并核对新增库存事件的业务来源与幂等键 |
| T+72h | `2026-08-06T09:48:30+08:00` | 待执行 | 同上，形成观察期关闭结论和残余异常清单 |

任一节点出现服务重启、迁移漂移、余额链断裂、孤儿关系、负余额、重复幂等键、无法解释的 `ADJUSTMENT` 或备份失败时，立即暂停后续阶段的生产发布；保留现场证据后按业务命令和前滚修复处理，不修改或删除既有台账流水。

V1 至 V3.49 历史 fixture 仍未取得。本观察记录不验证该历史升级链，也不把开账流水解释为切换日前历史事件。
